[tool.poetry]
name = "tix"
version = "0.1.0"
description = "A somewhat-silly simple time tracker"
authors = ["Your Name <you@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
dateparser = "^1.0.0"

[tool.poetry.dev-dependencies]
cram = "^0.7"
pytest-cram = "^0.2.2"
ipython = "^7.22.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
